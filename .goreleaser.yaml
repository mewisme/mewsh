version: 2

project_name: mewsh

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: mewsh
    main: .
    binary: mewsh
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/mewisme/mewsh/internal/version.Version={{.Version}}
      - -X github.com/mewisme/mewsh/internal/version.Commit={{.Commit}}
      - -X github.com/mewisme/mewsh/internal/version.Date={{.Date}}

archives:
  - id: mewsh
    ids:
      - mewsh
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md

checksum:
  name_template: checksums.txt

homebrew_casks:
  - name: mewsh
    ids:
      - mewsh
    binaries:
      - mewsh
    repository:
      owner: mewisme
      name: mewsh
      branch: main
      token: "{{ .Env.GITHUB_TOKEN }}"
    homepage: "https://github.com/mewisme/mewsh"
    description: "SSH profile manager with CLI and TUI"
    skip_upload: auto
    url:
      verified: github.com/mewisme/mewsh
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/mewsh"]
          end
    commit_author:
      name: github-actions[bot]
      email: 41898282+github-actions[bot]@users.noreply.github.com
    commit_msg_template: "brew: update {{ .ProjectName }} cask to {{ .Tag }}"

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: mewisme
    name: mewsh
  prerelease: auto
